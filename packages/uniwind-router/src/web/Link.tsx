/**
 * Web Link — re-exports TanStack Router's default Link component.
 *
 * On web, TanStack's Link renders an `<a>` tag with a proper href,
 * enabling cmd+click to open in new tab, right-click context menu,
 * SEO-friendly anchors, and proper browser history integration.
 */
export { Link } from '@tanstack/react-router'
